# Example environment variables for the React Native app.
# Copy this file to `.env` and fill in real values for local development.

# Third-party public API keys (safe to be in client, but still rotate/restrict).
GEOAPIFY_API_KEY=geoapify-example-key

# Optional: base URL for any custom backend you add later.
API_BASE_URL=https://api.example.com

# Enable extra debug logging in development (never true in production).
ENABLE_DEBUG_LOGGING=false
